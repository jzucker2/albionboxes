# Still want to convert to ethernet at some point
# pattern for naming
substitutions:
  node_name: pantryfoodcabinettempsensor
  friendly_name: Pantry Food Cabinet Temp Sensor
  area: Pantry
  comment: "Temperature sensors for the various shelves and cabinets in the pantry"
  box_config_file_name: prod_pantry_temp_sensor
  zeroth_sensor_name: "Pantry Food Cabinet Top Shelf"
  first_sensor_name: "Pantry Food Cabinet Espresso Machine Shelf"
  second_sensor_name: "Pantry Food Cabinet Bulk Storage Shelf"
  third_sensor_name: "Pantry Food Cabinet Microwave Shelf"
  fourth_sensor_name: "Pantry Food Cabinet Top Box Shelf"
  fifth_sensor_name: "Pantry Food Cabinet Bottom Box Shelf"
  oversampling_value: "16x"
  update_interval_value: "60s"
  sht_address: "0x44"
  bmp_address: "0x76"

packages:
  esphome: !include common/default_esphome.yaml
  wifi: !include common/wifi.yaml
  esp32: !include common/m5atom_device.yaml
  light: !include { file: common/atom_light.yaml, vars: { light_pin: 27 } }
  reset_button: !include common/reset_button.yaml
  networking: !include common/networking.yaml
  http: !include common/http.yaml
  version: !include common/version.yaml
  wifi_info: !include common/wifi_info.yaml
  uptime: !include common/uptime.yaml
  wifi_signal_sensor: !include common/wifi_signal_sensor.yaml
  status_sensor: !include common/status_sensor.yaml
  internal_temp: !include common/internal_temp.yaml
  dashboard_import: !include common/default_dashboard_import.yaml
  albion_boxes_info: !include common/albionboxes_info.yaml
  i2c: !include common/default_i2c.yaml
  button: !include { file: common/default_atom_button.yaml, vars: { button_pin: 39 } }

# temp sensors

# https://esphome.io/components/tca9548a
tca9548a:
  - address: 0x70
    id: multiplex0
    i2c_id: grove
    channels:
      - bus_id: multiplex0channel0
        channel: 0
      - bus_id: multiplex0channel1
        channel: 1
      - bus_id: multiplex0channel2
        channel: 2
      - bus_id: multiplex0channel3
        channel: 3
      - bus_id: multiplex0channel4
        channel: 4
      - bus_id: multiplex0channel5
        channel: 5

sensor:
  # https://esphome.io/components/sensor/sht4x.html
  - platform: sht4x
    i2c_id: multiplex0channel0
    address: ${sht_address}
    temperature:
      name: "${zeroth_sensor_name} Temperature"
    humidity:
      name: "${zeroth_sensor_name} Relative Humidity"
  # https://esphome.io/components/sensor/bmp280.html
  - platform: bmp280
    i2c_id: multiplex0channel0
    temperature:
      name: "${zeroth_sensor_name} Outside Temperature"
      oversampling: ${oversampling_value}
    pressure:
      name: "${zeroth_sensor_name} Outside Pressure"
    address: ${bmp_address}
    update_interval: ${update_interval_value}
  # https://esphome.io/components/sensor/sht4x.html
  - platform: sht4x
    i2c_id: multiplex0channel1
    address: ${sht_address}
    temperature:
      name: "${first_sensor_name} Temperature"
    humidity:
      name: "${first_sensor_name} Relative Humidity"
  # https://esphome.io/components/sensor/bmp280.html
  - platform: bmp280
    i2c_id: multiplex0channel1
    temperature:
      name: "${first_sensor_name} Outside Temperature"
      oversampling: ${oversampling_value}
    pressure:
      name: "${first_sensor_name} Outside Pressure"
    address: ${bmp_address}
    update_interval: ${update_interval_value}
  # https://esphome.io/components/sensor/sht4x.html
  - platform: sht4x
    i2c_id: multiplex0channel2
    address: ${sht_address}
    temperature:
      name: "${second_sensor_name} Temperature"
    humidity:
      name: "${second_sensor_name} Relative Humidity"
  # https://esphome.io/components/sensor/bmp280.html
  - platform: bmp280
    i2c_id: multiplex0channel2
    temperature:
      name: "${second_sensor_name} Outside Temperature"
      oversampling: ${oversampling_value}
    pressure:
      name: "${second_sensor_name} Outside Pressure"
    address: ${bmp_address}
    update_interval: ${update_interval_value}
  # https://esphome.io/components/sensor/sht4x.html
  - platform: sht4x
    i2c_id: multiplex0channel3
    address: ${sht_address}
    temperature:
      name: "${third_sensor_name} Temperature"
    humidity:
      name: "${third_sensor_name} Relative Humidity"
  # https://esphome.io/components/sensor/bmp280.html
  - platform: bmp280
    i2c_id: multiplex0channel3
    temperature:
      name: "${third_sensor_name} Outside Temperature"
      oversampling: ${oversampling_value}
    pressure:
      name: "${third_sensor_name} Outside Pressure"
    address: ${bmp_address}
    update_interval: ${update_interval_value}
  # https://esphome.io/components/sensor/sht4x.html
  - platform: sht4x
    i2c_id: multiplex0channel4
    address: ${sht_address}
    temperature:
      name: "${fourth_sensor_name} Temperature"
    humidity:
      name: "${fourth_sensor_name} Relative Humidity"
  # https://esphome.io/components/sensor/bmp280.html
  - platform: bmp280
    i2c_id: multiplex0channel4
    temperature:
      name: "${fourth_sensor_name} Outside Temperature"
      oversampling: ${oversampling_value}
    pressure:
      name: "${fourth_sensor_name} Outside Pressure"
    address: ${bmp_address}
    update_interval: ${update_interval_value}
  # https://esphome.io/components/sensor/sht4x.html
  - platform: sht4x
    i2c_id: multiplex0channel5
    address: ${sht_address}
    temperature:
      name: "${fifth_sensor_name} Temperature"
    humidity:
      name: "${fifth_sensor_name} Relative Humidity"
  # https://esphome.io/components/sensor/bmp280.html
  - platform: bmp280
    i2c_id: multiplex0channel5
    temperature:
      name: "${fifth_sensor_name} Outside Temperature"
      oversampling: ${oversampling_value}
    pressure:
      name: "${fifth_sensor_name} Outside Pressure"
    address: ${bmp_address}
    update_interval: ${update_interval_value}
